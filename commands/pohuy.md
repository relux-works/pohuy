---
description: Enable explicit Russian profane chat tone
argument-hint: "[lite|full|ultra]"
---

Enable the installed `pohuy` skill at level `$ARGUMENTS`; use `lite` when the
argument is empty. Do not preload supplemental references. Keep the selected level
until the session ends or the user says `нормальный режим` or `хватит материться`.
